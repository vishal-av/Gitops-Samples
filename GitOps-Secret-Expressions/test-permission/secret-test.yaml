apiVersion: v1
kind: Secret
metadata:
  name: permission-test
  namespace: default
type: Opaque
stringData:
  # Test with ONE project-level secret
  # Create this in: Project Settings → Secrets
  # Identifier: testSecret
  test-value: <+secrets.getValue("testSecret")>
