apiVersion: v1
kind: Service
metadata:
  name: sample-app
  namespace: devrel-pg
  labels:
    app: sample-app
spec:
  type: ClusterIP
  selector:
    app: sample-app
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
